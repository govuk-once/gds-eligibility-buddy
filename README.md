# Sequential agent: user eligibility with elicitation for user testing

## How do I run you?

In a terminal, navigate to my top-level directory and execute:

```bash
$ ./run.sh
```

This script will start both the main user agent and the remote eligibility agent, and
serve the Agent Development Kit Web UI at `http://localhost:8000`
